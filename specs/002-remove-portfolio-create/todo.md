# TODO / Backlog — Remove Portfolio Create Page

**Last updated**: 2026-02-25

## Open Items

- Frontend validation is pending: remaining open items are the frontend build (`cd web && npm run build`) and manual quickstart validation steps, blocked by missing `node`/`npm` in this environment.
